# Tona Adaptive Learning Upgrade

هذه النسخة تحافظ على استراتيجية SuperTrend والماسح الدوري كما هما، وتضيف طبقة تعلم تراكمي مستقلة.

## ما تم تغييره

1. **Adaptive Learning Engine**
   - يستفيد من التاريخ الكامل المتاح في Supabase، مع pagination بدل الاقتصار على 100/200 صفقة.
   - يبحث عن الحالات المشابهة حسب الأصل، BUY/SELL، RSI، ADX، MACD، الاتجاه، الحجم، RR ونظام السوق.
   - يستخدم وزنًا للحداثة حتى تتكيف الذاكرة مع تغير سلوك السوق دون حذف التاريخ.
   - يستخدم Bayesian shrinkage حتى لا تصبح عينة صغيرة سببًا لثقة زائفة.
   - ينتج احتمال نجاح، ثقة، حجم العينة الفعال، معدل الحالات المشابهة، expected profit، ومؤشر خطر الإشارة الكاذبة.

2. **إغلاق حلقة التوقع**
   - التوقع المسبق يستخدم نفس `trade_id` الحقيقي للصفقة.
   - عند إغلاق الصفقة يتم تحديث `trade_predictions` بالنتيجة الفعلية.
   - `was_correct` يقارن التوقع الفعلي بالنتيجة الفعلية، وليس فقط بكون الصفقة رابحة.
   - تتم إعادة معايرة التوقعات بعد الإغلاق.

3. **التعلم أثناء المراقبة**
   - كل دورة مراقبة تعيد تقييم احتمال النجاح/الفشل من الذاكرة التاريخية.
   - لا تغير SuperTrend ولا قرار الماسح.
   - تحذير مستقل يظهر فقط عند وجود خطر تاريخي مرتفع لتجنب إغراق Telegram.

4. **إصلاح profit_pct**
   - أصبح حساب الربح النسبي صحيحًا للـBUY والـSELL في الإغلاق، المراقبة، والتحذيرات.

5. **تحسين اكتشاف الأنماط**
   - رفع الحد الأدنى للنمط من 3 إلى 8 عينات.
   - رفع شروط الدعم والـlift لتقليل الأنماط الوهمية.
   - حفظ الأنماط عبر طبقة Supabase الموحدة في `main.py`.

## ملاحظة مهمة

الصفقات التاريخية القديمة تستخدم كبيانات تدريب/حالات مشابهة حتى لو لم يكن لها prediction سابق. لن يتم اختلاق توقعات تاريخية غير موجودة.

ملف `omniscient_core_v5.py` الموجود في المشروع الأصلي يحتوي على حرف عربي منفرد في أول الملف ويسبب SyntaxError إذا حاول Python compile-all للمشروع؛ لم أعدله لأنه ليس جزءًا من مسار الاستيراد المستخدم في `main.py`.
