# Tona AI — Adaptive Super Learning V15

هذه النسخة تحافظ على استراتيجية اكتشاف الصفقات SuperTrend + VPT والماسح الدوري كما هي، وتضيف/تقوي طبقة التعلم والتنبؤ فقط.

## أهم المبادئ
- لا يتم تعديل إشارة SuperTrend الأصلية.
- لا يتم استخدام بيانات مستقبلية من الـSnapshots لصناعة توقع لحظة الدخول.
- الصفقات التاريخية المغلقة في `trades_full` هي الذاكرة الأساسية.
- الحالات المشابهة تُوزن بالتشابه والحداثة.
- توجد أولوية خاصة للأصل + اتجاه الصفقة قبل fallback إلى التاريخ العام.
- Breakeven ليس نجاحاً عند تدريب احتمال "صفقة رابحة".
- الثقة منفصلة عن الاحتمال وترتبط بحجم الدليل.
- معايرة التوقعات لا تعمل إلا بعد توفر عدد كافٍ من التوقعات المكتملة.
- عند الإغلاق يتم ربط `actual_outcome` و`was_correct` بنفس `trade_id`.
- المراقبة التكيفية كل 5 دقائق لا تغير SuperTrend؛ هي تعيد تقدير خطر الفشل فقط.

## المطلوب بعد التشغيل
راقب أن الصفقة الجديدة تظهر بنفس `trade_id` في:
- `trades_full`
- `trade_predictions`
- `snapshots`
- `lessons_deep`

ثم بعد إغلاق عدة صفقات، راجع `was_correct` و`confidence` و`false_signal_score` للتأكد من أن حلقة التعلم أغلقت بشكل صحيح.
